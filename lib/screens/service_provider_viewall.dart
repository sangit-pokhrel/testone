import 'package:flutter/material.dart';

class ServiceProviderViewall extends StatelessWidget {
  const ServiceProviderViewall({super.key, required List<Map<String, String>> providers});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
